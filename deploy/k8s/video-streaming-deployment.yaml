apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: flixtube
  name: video-streaming
spec:
  replicas: 1
  selector:
    matchLabels:
      app: video-streaming
  template:
    metadata:
      labels:
        app: video-streaming
    spec:
      containers: 
      - name: video-streaming
        image: crazydanila/video-streaming:df54efa99990154a7956c317554fdd74fffcaba1
        imagePullPolicy: IfNotPresent
        ports:
        - name: streaming-port
          containerPort: 4006
        env:
        - name: PORT
          value: "4006"
        - name: RABBIT
          value: "amqp://guest:guest@rabbit:5672"
        resources:
          limits:
            cpu: "0.5"
            memory: "512Mi"
          requests:
            cpu: "0.1"
            memory: "256Mi"
        readinessProbe:
          httpGet:
            path: /api/live
            port: 4006
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  namespace: flixtube
  name: video-streaming
spec:
  selector:
    app: video-streaming
  ports:
    - protocol: TCP
      port: 80
      targetPort: streaming-port